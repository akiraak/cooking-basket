#!/bin/sh
set -e

repo_root=$(git rev-parse --show-toplevel)

echo "[pre-commit] server tests"
(cd "$repo_root/server" && npm test --silent)

echo "[pre-commit] mobile tests"
(cd "$repo_root/mobile" && npm test --silent)
